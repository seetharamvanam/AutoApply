# Copy this file to ".env" and fill in values for local development.
#
# The backend reads ".env" at startup (see com.autoapply.UnifiedServiceApplication)
# and maps entries into system properties if they are not already set.

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=autoapply
DB_USERNAME=postgres
DB_PASSWORD=your_postgres_password_here

# Optional: full JDBC URL (overrides host/port/name if set)
# DB_URL=jdbc:postgresql://localhost:5432/autoapply

# JWT (use a long random secret; minimum 256 bits / 32+ chars recommended)
JWT_SECRET=change-me-in-dev-too

# Frontend URL (used for redirects/emails; optional)
FRONTEND_URL=http://localhost:3000

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,chrome-extension://*,moz-extension://*,edge-extension://*
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS

# Optional: Redis (if enabled)
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Optional: Mail (for password reset emails)
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=
# MAIL_PASSWORD=


